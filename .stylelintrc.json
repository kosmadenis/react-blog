{
  "extends": ["stylelint-config-standard-scss"],
  "overrides": [
    {
      "files": ["**/*.scss"],
      "customSyntax": "postcss-scss"
    }
  ],
  "rules": {
    "selector-class-pattern": [
      "^([a-z][A-Z0-9]*)+$",
      { "message": "Use camelCase for class selectors" }
    ]
  }
}
